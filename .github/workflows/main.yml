name: CI
on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
        python-version: '3.x'
        architecture: 'x64'
    - run: |
        cd tests
        python run_all_tests.py
